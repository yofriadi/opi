//! Web UI components.

pub struct ChatWidget;

impl ChatWidget {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ChatWidget {
    fn default() -> Self {
        Self::new()
    }
}
