/target
.repo

# Claude Code local-only state
.claude
!.claude/skill/opi-implement
!.claude/skill/opi-release
.agents

# Secrets / credentials (must never be committed)
.env
.env.*
*.key
*.pem
*.p12
*.pfx
credentials.toml

# Local project config (may contain provider base_url, model overrides)
.opi/config.toml

# Release skill resume state
.opi-release-state.json

# Release artifacts (uploaded to GitHub Release, not tracked)
release-notes.md
/release-artifacts/

# opi-implement runtime state (never committed)
.opi-impl-state.json
.opi-impl-state.json.tmp
.opi-impl-state.draft.json
# Ignore local notes/plans
PLAN.md
PI_RUST_PORT_COMPARISON.md

